<div align="center">

# ✨ SmoothLoader

**Плавные и стильные индикаторы загрузки для ваших React приложений**

[![npm version](https://img.shields.io/npm/v/react-smooth-loader?style=flat-square)](https://www.npmjs.com/package/react-smooth-loader)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg?style=flat-square)](https://opensource.org/licenses/MIT)
[![TypeScript](https://img.shields.io/badge/TypeScript-Ready-blue?style=flat-square&logo=typescript)](https://www.typescriptlang.org/)

*Один компонент — множество стилей анимации. Просто, быстро, красиво.*

</div>

---

## 🎯 Что это?

`SmoothLoader` — это легковесная библиотека компонентов загрузки для React. Вместо того чтобы искать готовые решения или писать свои спиннеры с нуля, просто используйте `<SmoothLoader />` и выберите один из 26 встроенных стилей анимации.

### ✨ Особенности

- 🎨 **26 уникальных стилей** анимации на выбор
- 🎨 **Полная кастомизация** — размер, цвет, классы
- 📦 **Легковесная** — минимум зависимостей
- 🔷 **TypeScript** — полная поддержка типов из коробки
- ⚡ **Быстрая** — оптимизированные CSS анимации
- 🎯 **Простая** — один компонент, все работает
- 🌐 **Широкая поддержка браузеров** — работает во всех современных браузерах, включая IE11

---

## 🚀 Быстрый старт

### Установка

```bash
npm install react-smooth-loader
```

или с yarn:

```bash
yarn add react-smooth-loader
```

### Использование

```tsx
import { SmoothLoader } from 'react-smooth-loader';

function MyComponent() {
  return <SmoothLoader />;
}
```

Вот и всё! 🎉

---

## 📚 Документация

### Пропсы

| Проп | Тип | По умолчанию | Описание |
|:----:|:---:|:------------:|----------|
| `height` | `number \| string` | `50` | Высота компонента (px или строка) |
| `width` | `number \| string` | `50` | Ширина компонента (px или строка) |
| `type` | `LoaderType` | `'spinner'` | Тип анимации (см. ниже) |
| `color` | `string` | `'#3b82f6'` | Цвет спиннера (hex, rgb, название) |
| `className` | `string` | `''` | Дополнительные CSS классы |

### 🎭 Доступные типы анимации

| Тип | Описание | Когда использовать |
|:---:|----------|-------------------|
| `spinner` | Классический вращающийся круг | Универсальный вариант |
| `dots` | Три пульсирующие точки | Минималистичный стиль |
| `pulse` | Расходящиеся круги | Для больших экранов |
| `wave` | Волнообразные полоски | Современный вид |
| `circle` | Концентрические кольца | Элегантный вариант |
| `bounce` | Подпрыгивающие точки | Игривый стиль |
| `cat` | Кот с подушкой на шее 🐱 | Когда нужен милый и необычный загрузчик |
| `bars` | Горизонтальные пульсирующие полоски | Для горизонтальных интерфейсов |
| `grid` | Сетка из мигающих точек | Для современных дизайнов |
| `ring` | Множественные вращающиеся кольца | Для сложных интерфейсов |
| `orbit` | Точки, вращающиеся по орбите | Для космических тем |
| `ripple` | Волны, расходящиеся от центра | Для водных/жидких эффектов |
| `clock` | Часы с вращающимися стрелками | Для временных процессов |
| `infinity` | Символ бесконечности | Для бесконечных циклов |
| `spiral` | Вращающаяся спираль | Для прогрессивной загрузки |
| `cube` | 3D вращающийся куб | Для объемных интерфейсов |
| `heartbeat` | Пульс/сердцебиение | Для медицинских/жизненных тем |
| `star` | Вращающаяся звезда | Для космических/премиум тем |
| `morphing` | Плавно трансформирующиеся формы | Для футуристических интерфейсов |
| `liquid` | Жидкий эффект с волнами | Для динамичных и современных дизайнов |
| `neon` | Неоновое свечение | Для темных тем и киберпанк стиля |
| `particle` | Орбитальные частицы | Для научных и технологических проектов |
| `dna` | Двойная спираль ДНК | Для биомедицинских и научных приложений |
| `zen` | Медитативный круг | Для спокойных и минималистичных интерфейсов |
| `glitch` | Глитч эффект с искажениями | Для киберпанк и ретро-футуристических тем |
| `plasma` | Плазменный эффект с переливами | Для футуристических и энергетических интерфейсов |

---

## 💡 Примеры использования

### Базовый пример

```tsx
import { SmoothLoader } from 'react-smooth-loader';

function LoadingScreen() {
  return (
    <div className="loading-container">
      <SmoothLoader />
    </div>
  );
}
```

### Кастомные размеры

```tsx
// Маленький спиннер
<SmoothLoader height={30} width={30} />

// Большой спиннер
<SmoothLoader height={100} width={100} />

// Адаптивный размер
<SmoothLoader height="5vh" width="5vw" />
```

### Разные типы анимации

```tsx
<SmoothLoader type="spinner" />   // Классика
<SmoothLoader type="dots" />      // Минимализм
<SmoothLoader type="pulse" />     // Эффектность
<SmoothLoader type="wave" />       // Современность
<SmoothLoader type="circle" />     // Элегантность
<SmoothLoader type="bounce" />    // Игривость
<SmoothLoader type="cat" />       // Мяу! 🐱
<SmoothLoader type="bars" />      // Горизонтальный
<SmoothLoader type="grid" />      // Сетка
<SmoothLoader type="ring" />      // Кольца
<SmoothLoader type="orbit" />     // Орбита
<SmoothLoader type="ripple" />    // Волны
```

### Кастомные цвета

```tsx
// Hex цвета
<SmoothLoader type="spinner" color="#ff6b6b" />

// RGB
<SmoothLoader type="dots" color="rgb(34, 197, 94)" />

// Названия цветов
<SmoothLoader type="pulse" color="purple" />
<SmoothLoader type="wave" color="coral" />
```

### Полный пример с настройками

```tsx
import { SmoothLoader } from 'react-smooth-loader';

function App() {
  return (
    <div className="app">
      <SmoothLoader 
        height={80} 
        width={80} 
        type="wave" 
        color="#8b5cf6"
        className="my-loader"
      />
    </div>
  );
}
```

### Использование с условным рендерингом

```tsx
function DataComponent() {
  const [loading, setLoading] = useState(true);
  const [data, setData] = useState(null);

  if (loading) {
    return (
      <div className="centered">
        <SmoothLoader type="spinner" height={60} width={60} />
        <p>Загрузка данных...</p>
      </div>
    );
  }

  return <div>{/* Ваш контент */}</div>;
}
```

---

## 🔷 TypeScript

Библиотека полностью типизирована. Все типы доступны для импорта:

```tsx
import { SmoothLoader, LoaderType } from 'react-smooth-loader';

// Типизированный выбор типа
const loaderType: LoaderType = 'spinner';

// Автодополнение в IDE работает из коробки
<SmoothLoader type="spinner" /> // ✅
<SmoothLoader type="invalid" /> // ❌ TypeScript ошибка
```

---

## 🎨 Стилизация

Вы можете добавить свои CSS классы через проп `className`:

```tsx
<SmoothLoader 
  className="my-custom-loader"
  type="spinner"
/>
```

```css
.my-custom-loader {
  margin: 20px auto;
  opacity: 0.8;
}
```

---

## 🤝 Вклад в проект

Мы приветствуем любые вклады! Если у вас есть идеи для новых типов анимации или улучшений — создавайте issue или pull request.

---

## 🌐 Поддержка браузеров

Библиотека поддерживает все современные браузеры и IE11:

- ✅ Chrome (последние 2 версии)
- ✅ Firefox (последние 2 версии + ESR)
- ✅ Safari (последние 2 версии)
- ✅ Edge (последние 2 версии)
- ✅ Opera (последние 2 версии)
- ✅ IE 11
- ✅ Мобильные браузеры (iOS Safari, Chrome Mobile)

Код автоматически транспилируется в ES2015 и включает необходимые полифиллы для старых браузеров. CSS автоматически получает необходимые префиксы через Autoprefixer.

---

## 📄 Лицензия

MIT License — используйте свободно в любых проектах.

---

<div align="center">

**Сделано с ❤️ для React сообщества**

⭐ Если проект вам понравился, поставьте звезду!

</div>
